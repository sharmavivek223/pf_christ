import React, { Fragment } from "react";
import NavBar from "./Shared/NavBar";

const ErrorPage = () => {
  return (
    <Fragment>
    <NavBar/>
    </Fragment>
  );
};

export default ErrorPage;
